#!/usr/bin/env bash
# inkpost-serve — the local web ui on http://localhost:8788
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
PY="$DIR/.venv/bin/python"
[ -x "$PY" ] || PY="$(command -v python3)"
exec "$PY" "$DIR/server.py" "$@"
