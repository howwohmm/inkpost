# internal notes

These files come from the build of inkpost. They are kept for context, not as
documentation.

- `SPEC.md` — the original build spec. Parts of it no longer match the code. The
  prompts in `planner.py` were rewritten after it was written.
- `BRIEF.md` — the house rules for what makes a good diagram. Still accurate.
- `D2-REFERENCE.md` — a cheatsheet of d2 themes, shapes, styles and export flags.

For current documentation, read `README.md`, `CONTRIBUTING.md`, `docs/PATTERNS.md`
and `docs/MODES.md`.
